/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configurazione suggerita dai log per sbloccare HMR
  allowedDevOrigins: ['127.0.0.1', 'localhost', '100.108.195.2'],
};

module.exports = nextConfig;
